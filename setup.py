# Following PEP 517/518, this file should not be needed since it is replaced by the setup.cfg and pyproject.toml files.
# However, it is still required by the pip editable mode `pip install -e`
# See https://stackoverflow.com/a/60885212

from setuptools import setup

if __name__ == "__main__":
    setup()
